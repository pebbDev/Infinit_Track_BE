// Placeholder seeder for FAHP TFN matrices
// Future: persist to DB table decision_profiles

export async function seedDefaultFahpProfiles(_db) {
  // no-op placeholder; reads from config.fahp.js for now
  return { seeded: false, source: 'config.fahp.js' };
}
